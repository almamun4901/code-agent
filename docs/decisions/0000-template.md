# NNNN — <short decision title>

**Date:** YYYY-MM-DD
**Status:** proposed | accepted | superseded by NNNN

## Context

What problem or fork in the road prompted this decision? What constraints
(time, budget, rubric weighting, tooling availability) shaped the options?

## Decision

The one or two sentences that state what was chosen. Not the reasoning —
just the choice, stated plainly enough to quote elsewhere.

## Alternatives considered

- **Option A** — why it was passed over
- **Option B** — why it was passed over

## Consequences

What this makes easier, what it makes harder, what it defers or forecloses.

## Revisit when

The concrete trigger condition under which this decision should be
reopened — not "if it doesn't work out," but a specific signal
(e.g. "if E2B's sandbox image is missing ripgrep with no override path").
